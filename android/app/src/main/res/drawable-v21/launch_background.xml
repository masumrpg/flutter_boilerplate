<?xml version="1.0" encoding="utf-8"?>
<!-- This drawable is used as the background during app launch (before Flutter renders).
     Keep it simple so the transition to Flutter's SplashPage is seamless. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@android:color/white" />
</layer-list>
